/**
 * name convention
 *
 * 1) 일반적으로 영어(라틴문자)를 사용하며 문자와 숫자를 사용할 수 있음
 * 2) 특기호는 언더스코어와 달러를 사용할 수 있음
 * 3) 숫자로 시작하면 안 됨
 * 4) 키워드는 변수명으로 사용할 수 없음
 */
let king = `king`;
let $halo = `halo`;
let _kingu = `kingu`;

console.log(king, $halo, _kingu);

/**
 * 1) camelCase - 대부분의 언어에서 사용
 * 2) snake_case - python
 * 3) PascalCase - C# 마이크로소프트 계열의 언어에서 사용
 */

const kingHalo = `king halo`;
